import styled from 'styled-components';
import { size } from '../css/breakpoint';

const Container = styled.div`
  max-width: ${size.lg};
  margin-left: auto;
  margin-right: auto;
`;

export default Container;
